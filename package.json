{
  "name": "zowe-sample-client-templates",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no root tests\" && exit 1",
    "lint": "eslint . --ext ts",
    "prettier": "prettier packages/*/src/**/*.ts --write",
    "compile": "tsc -b",
    "license": "license.sh",
    "build": "npm run license && npm run prettier && npm run compile && npm run lint && npm test --workspaces",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "EPL-2.0"
}
